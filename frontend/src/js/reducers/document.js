
const initialState={
    document : {},
    loading : true,
  
}

export default function (state= initialState , action){
    const  {type , payload} = action
    switch ("key") {
        case "value":
            
            break;
    
        default:
            return state
    }
}